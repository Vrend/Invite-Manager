# Invite-Manager
Manage invites and provide an easy to use QR code for check-in

## Install

Begin by setting up a virtual environment by running the following commands:
 1. `cd Invite-Manager`
 2. `python3 -m venv venv`
 3. `pip install Flask`